program Test1 {

    int sum(int n) int i, s;{       
        s = 0;

        for(i = 0; i < n; i = i + 1) {
            if(i % 2 == 0)
                s = s + i;
        }

        return s;
    }

    void main() int result; {
        result = sum(10);
        print(result);
    }
}
